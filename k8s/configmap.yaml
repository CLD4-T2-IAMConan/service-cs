apiVersion: v1
kind: ConfigMap
metadata:
  name: cs-config
data:
  db.host: "postgres-service"
  db.port: "5432"
  db.name: "cs_db"
  application.yml: |
    spring:
      application:
        name: cs-service
      jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
